def MergeSort(list, left, right):
    middle = (left + right) / 2
    MergeSort(list, left, middle)


def main():
    list[10, 21, 3, 4, 0, 2]
    len = len(list)
    MergeSort(list, list[0], list[len - 1])


if __name__ == "__main__":
    main()
